server:
  port: 8888

spring:
  application:
    name: kah-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jhemder/microservicios-matricula-kahh.git
          default-label: main
